/**
 * These functions are identical to the standard functions provided by
 * {@link java.util.function}, but their methods are instead called "call".
 * This lets us use a feature from Groovy where you can call a method directly, if its name is "call".
 */
package com.atherys.script.api.function;